# (Done)[2024_07_16]1순위 버그 수정의 건

생성일: 2024년 7월 16일 오전 10:38
AI 사용자 지정 자동 채우기: This document is titled "(Done)[2024_07_16]1순위 버그 수정의 건" and contains a detailed report on a bug fix related to the spawning process of the insect player in a game. It identifies an unnecessary RPC (Remote Procedure Call) that was causing synchronization failures. The report includes code snippets where changes were made to process certain actions directly instead of using RPCs, which could lead to potential side effects, such as avatar IK (inverse kinematics) synchronization issues.

Additionally, the document discusses the implementation of adding missions, explaining how roles are assigned based on certain conditions using a boolean flag. It also details how to handle player missions at different levels, including the creation of low and middle-level missions, and the debug logs that indicate the completion of these actions. There are mentions of using Photon RPC functions to ensure avatars are activated correctly, with a code snippet demonstrating the use of ownership checks in the FixedUpdate method.

Overall, the document highlights various changes in the code to improve functionality and outlines potential side effects of these modifications.
AI 요약: 곤충채집 게임의 버그 수정 사항으로, RPC 호출 대신 직접 처리로 변경하여 아바타 IK 동기화 문제를 해결하고, 미션 추가 시 역할 구분을 위한 코드 수정이 이루어졌습니다. 포톤 RPC 기능을 활용하여 아바타 활성화 문제를 방지하는 방법도 설명되었습니다.

![Untitled]((Done)%5B2024_07_16%5D1%EC%88%9C%EC%9C%84%20%EB%B2%84%EA%B7%B8%20%EC%88%98%EC%A0%95%EC%9D%98%20%EA%B1%B4/Untitled.png)

곤충채집 insect_player의 스폰과정에서 쓸데없는 RPC 발견 (RPC 될 시에 동기화 failed된 듯함)

```csharp

    public void active_01() {
        //끄고 키기
        // pv.RPC("RPC_mun_01", RpcTarget.AllBuffered);
        // Invoke("active_02", 1f);
        active_02();
    }

    public void active_02() {
        // pv.RPC("RPC_mun_02", RpcTarget.AllBuffered);
        
        if (!b_ai) {

            if (!pv.IsMine) {

                Avator_tr.gameObject.SetActive(true);
                Avator_obj.GetComponent<VRIK>().solver.Reset();
            }
            else
            {
                Avator_tr.gameObject.SetActive(false);
            }

        }

    }

    [PunRPC]
    public void RPC_mun_01() {
         Avator_tr.gameObject.SetActive(false);
        Avator_obj.GetComponent<VRIK>().solver.Reset();

    }
 
    [PunRPC]
    public void RPC_mun_02() {
        if (!b_ai) {

            if (!pv.IsMine) {

                Avator_tr.gameObject.SetActive(true);
                Avator_obj.GetComponent<VRIK>().solver.Reset();
            }

        }

    }
```

rpc가 아니라 active_02에서 바로 처리되게 변경하였음.

예상 sideEffect → 아바타 IK가 동기화 안될 수도 있습니다

![Untitled]((Done)%5B2024_07_16%5D1%EC%88%9C%EC%9C%84%20%EB%B2%84%EA%B7%B8%20%EC%88%98%EC%A0%95%EC%9D%98%20%EA%B1%B4/Untitled%201.png)

mission_add시 역할 바뀌기 코드가 없음.

```csharp
 public void Mission_add() {
        Debug.Log(Launcher.Instance.levle.ToString() + "단계를 미션생성 시작하겠습니다.");

        if (insect_mission.Instance.playerMissions.Count-1 != insect_player_manager.Instance.Players.Length) {
            insect_player_manager.Instance.renewal_info(0);
        }

        if (Launcher.Instance.m_levle >= 1 && Launcher.Instance.m_levle <= 8) {
            for (int i = 0; i < insect_player_manager.Instance.players_Info.Count; i++) {
                PlayerMission mission = new PlayerMission();
                string Mission_title = "";

                int Random_ = 0;//UnityEngine.Random.Range(0, insects.Count);

                //low 미션
                List<low_mission> lowmission = new List<low_mission>();
                low_mission low = new low_mission();

                //middle 미션

                middle_missoin middle = new middle_missoin();
                insect_middle_role rolo_type;
                dir_type dir_;
                int count = i;
                
                //여기를 변경 하였음 =>RJH
                if(isAlreadyMissionAdd)
                {
                    count ++;
                    isAlreadyMissionAdd = false;
                }
                else
                {
                    isAlreadyMissionAdd = true;
                }
                bool my_role = sniffling(count); //홀수면 선공 혹은 잡기 , 짝수면 후공 바꾸기
                
                if (my_role) //홀수잡기
                {
                    rolo_type = insect_middle_role.잡기;
                    dir_ = dir_type.왼쪽;
                }
                else {
                    rolo_type = insect_middle_role.바꾸기;
                    dir_ = dir_type.오른쪽;
                };
                //여기까지
                //Middle 미션 종료

                string[] parts = new string[2];
                switch (Launcher.Instance.levle) {
                    case Level.Levle_1:
                        Mission_title = "&&오브젝트 잡기";
                        mission.Color = new string[color_count];
                        //  int Random_ = UnityEngine.Random.Range(0, Insects.Count);
                        for (int j = 0; j < insects.Count; j++) {
                            Debug.Log(j + "ddd");
                            //  pv.RPC("Low_share_mission_RPC", RpcTarget.AllBuffered, players_Info[i].NickName, Insects[j].name);

                            parts = insects[j].name.Split('_');
                            mission.obj_name = parts[0];
                            mission.Color[j] = parts[1];
                            mission.dir = dir_.ToString();
                            low = insect_mission.Instance.low(insects[j].name);
                            lowmission.Add(low);
                        }
                        mission.low = lowmission;

                        break;
                    case Level.Levle_2:
                        mission.Color = new string[1];
                        //역순 정렬
                        ReverseList(insects);
                        for (int j = 0; j < 1; j++) {
                            parts = insects[j].name.Split('_');
                            mission.obj_name = parts[0];
                            mission.Color[j] = parts[1];
                            mission.dir = dir_.ToString();
                            low = insect_mission.Instance.low(insects[j].name);
                            lowmission.Add(low);
                        }
                        mission.low = lowmission;
                        break;
                    case Level.Levle_3:

                        mission.Color = new string[1];
                        low = insect_mission.Instance.low(insects[Random_].name);
                        parts = insects[Random_].name.Split('_');
                        mission.obj_name = parts[0];
                        mission.Color[0] = parts[1];
                        mission.dir = dir_.ToString();
                        lowmission.Add(low);
                        mission.low = lowmission;
                        break;
                    case Level.Levle_4:
                        mission.Color = new string[2];
                        //역순 정렬
                        ReverseList(insects);
                        for (int j = 0; j < 2; j++) {
                            parts = insects[j].name.Split('_');
                            mission.obj_name = parts[0];
                            mission.Color[j] = parts[1];
                            mission.dir = dir_.ToString();
                            low = insect_mission.Instance.low(insects[j].name);
                            lowmission.Add(low);
                        }
                        mission.low = lowmission;

                        break;
                    case Level.Levle_5:
                        mission.Color = new string[1];
                        middle = insect_mission.Instance.middle(rolo_type, insects[Random_].name, dir_);
                        parts = insects[Random_].name.Split('_');
                        mission.obj_name = parts[0];
                        mission.Color[0] = parts[1];
                        mission.dir = dir_.ToString();
                        mission.middle = middle;
                        break;
                    case Level.Levle_6:
                        mission.Color = new string[1];
                        middle = insect_mission.Instance.middle(rolo_type, insects[Random_].name, dir_);
                        parts = insects[Random_].name.Split('_');
                        mission.obj_name = parts[0];
                        mission.Color[0] = parts[1];
                        mission.dir = dir_.ToString();
                        mission.middle = middle;
                        break;
                    case Level.Levle_7:
                        mission.Color = new string[1];
                        Debug.Log(i + "플레이어 미션");
                        string[] d_parts_1 = insects[0].name.Split('_');
                        string[] d_parts_2 = insects[1].name.Split('_');

                        if (insect_player_manager.Instance.Players[i].GetComponent<insect_player>().ActorNr_num == 2)
                        {
                            if (d_parts_1[1] == data_cs.Color_name[6]) // 첫번째 오브젝트가 화이트일대
                            {
                                middle = insect_mission.Instance.middle(rolo_type, insects[1].name, dir_);
                                parts = insects[1].name.Split('_');

                            }
                            else
                            {
                                middle = insect_mission.Instance.middle(rolo_type, insects[0].name, dir_);
                                parts = insects[0].name.Split('_');
                            }

                        }
                        else if (insect_player_manager.Instance.Players[i].GetComponent<insect_player>().ActorNr_num == 3)
                        {
                            if (d_parts_1[1] == data_cs.Color_name[6]) // 첫번째 오브젝트가 화이트일대
                            {
                                middle = insect_mission.Instance.middle(rolo_type, insects[0].name, dir_);
                                parts = insects[0].name.Split('_');
                            }
                            else
                            {
                                middle = insect_mission.Instance.middle(rolo_type, insects[1].name, dir_);
                                parts = insects[1].name.Split('_');
                            }

                        }
                        mission.obj_name = parts[0];
                        mission.Color[0] = parts[1];
                        mission.dir = dir_.ToString();
                        mission.middle = middle;
                        break;
                    case Level.Levle_8:
                        mission.Color = new string[1];
                        middle = insect_mission.Instance.middle(rolo_type, insects[i].name, dir_);
                        parts = insects[i].name.Split('_');
                        mission.obj_name = parts[0];
                        mission.Color[0] = parts[1];
                        mission.dir = dir_.ToString();
                        mission.middle = middle;
                        break;

                    default:
                        break;
                }
                //  insect_mission.Instance.CreateMission(insect_player_manager.Instance.players_Info[i].NickName, Mission_title);

                //insect_mission.Instance.playerMissions[i] = mission;
                mission.playerName = insect_player_manager.Instance.Players[i].GetComponent<insect_player>().info.NickName;
                mission.missionDetail = Mission_title;
                //   Debug.Log(insect_player_manager.Instance.players_Info[i].player_obj.GetComponent<PhotonView>().OwnerActorNr.ToString());
                mission.ActorNumber = insect_player_manager.Instance.Players[i].GetComponent<insect_player>().ActorNr_num.ToString();
                //insect_mission.Instance.playerMissions.Add(mission);
                insect_mission.Instance.playerMissions[i] = mission;
                Debug.Log("low 미션 생성완료");

            }
        }
        //////////////////////////////////////////////high 레벨 미션//////////////////////////////////////
        else if (Launcher.Instance.m_levle >= 9 && Launcher.Instance.m_levle <= 12) {
            AI_add();
            return;
        }

        Debug.Log("미션 생성 함수 종료 데이터 전체 전송 이다음 나레이션 출력");
        //insect_mission.Instance.SendDataToPlayer();
        insect_GameplayManager.Instance.Master_funcion(1);
        ///////////////////////////////////////////////// 나레이션 //////////////////////////
        Debug.Log("Low ~ Middle 나레이션 출력?");
        //Insect_Sound.Instance.Levle_Mission_Sound();

        for (int i = 0; i < insect_player_manager.Instance.players_Info.Count; i++) {
            insect_player_manager.Instance.Players[i].GetComponent<insect_player>().RPC_Play_ner();
            insect_player_manager.Instance.Players[i].GetComponent<insect_player>().active_01();
          //  insect_player_manager.Instance.players_Info[i].player_obj.GetComponent<insect_player>().RPC_Play_ner();
          //  insect_player_manager.Instance.players_Info[i].player_obj.GetComponent<insect_player>().active_01();

        }

       // Invoke("start_nar", 15f);
        Debug.Log("로우레벨  나레이션 출력?");

    }
```

bool flag 하나 추가하여 번갈아서 역할 구분이 되도록 추가하였습니다.

예상 sideEffect→ 없음

![Untitled]((Done)%5B2024_07_16%5D1%EC%88%9C%EC%9C%84%20%EB%B2%84%EA%B7%B8%20%EC%88%98%EC%A0%95%EC%9D%98%20%EA%B1%B4/Untitled%202.png)

포톤 RPC 펑션을 놓치면 아바타가 활성화 안될수도 있음 

fixedupdate에 아바타를 포톤view ownership을 활용해서 적용

```csharp
     private void FixedUpdate() {
        Avator_obj.gameObject.SetActive(!pv.IsMine);
        
    }

```

예상 sideEffect →없음